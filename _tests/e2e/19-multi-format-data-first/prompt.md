First resolve branding (run the brand skill to write .reporting-resolved/), then: I need a Q1 2026 performance report in multiple formats from the same data. Revenue was $12.4M (+8.2%), EBITDA $3.1M (+12.1%), Headcount 142, NPS 72.

Generate all of these:
1. First, a structured JSON data file as the canonical data source
2. A PDF executive summary from that data
3. A CSV export of the quarterly numbers

All three must show identical numbers. Write everything to the output directory.
